# Resource scripts are UTF-16LE. Treat as binary so git never rewrites them.
*.rc  -text diff
*.rc2 -text diff

# MSVC project files stay CRLF
*.sln     -text
*.vcproj  -text
*.vcxproj -text
*.vcxproj.filters -text
*.wxs     -text
*.wxi     -text
